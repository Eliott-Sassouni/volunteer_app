<div id="tf-menachem" class="text-center whatever">
	<div class="overlay events-page">
<!-- <div id="tf-contact" class="text-center"> -->
	<div class="container event-title">

	    <div class="row">
	        <div class="col-md-8 col-md-offset-2">

	            <div class="section-title center">				

					<h1><%= @event.name %></h1><br />
					<h3> This event is coordinated by: </h3><br />
					<h2><%= link_to @event.user.profile.full_name.upcase, profile_path(@event.user.profile) %></h2><br /><br />

					<img src="<%= @event.photo %>", class="image-size"><br /><br />

					<h4> About this event: </h4>
					<p><%= @event.description %></p><br />

					<h4> Location of event: </h4>
					<p><%= @event.location %></p><br /><br />

					<h2>Comments:</h2><br />
					<% @event.comments.each do |comment| %>
						<p>
					    	<%= comment.comment %> | <%= comment.commenter %>
					    	<% if (current_user.profile.full_name == comment.commenter) || (current_user.id == @event.user_id) %>
					    		 | <%= link_to 'Delete', event_comment_path(@event, comment),
						            method: :delete,
						            data: { confirm: 'Are you sure?' } %>
							<% end %>
						</p>
					<% end %>

					<div class="comment-text"><h2>Add a comment:</h2></div>

					<div class="comment-color"><%= render "comments/form" %>
					</div>

					<%= link_to 'All Events', events_path %>
					<% if @event.user == current_user %>
						| <%= link_to 'edit', edit_event_path(@event) %>
						| <%= link_to 'delete', event_path(@event),
						            method: :delete,
						            data: { confirm: 'Are you sure?' } %>
					<% end %>
				</div>
			</div>
		</div>
	</div>
</div>